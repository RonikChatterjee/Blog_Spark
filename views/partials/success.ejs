<div class="success-backdrop hidden" id="success-backdrop">
    <div class="success-container" id="success-container">
        <!-- Success Icon -->
        <div class="success-icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" />
                <path d="m9 12 2 2 4-4" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                    stroke-linejoin="round" />
            </svg>
        </div>

        <!-- Error Header -->
        <div class="success-header">
            <h3 class="success-title" id="success-title">
            </h3>
        </div>

        <!-- Success Message -->
        <div class="success-content">
            <p class="success-message" id="success-message">
            </p>
        </div>

        <!-- Success Actions -->
        <div class="success-actions">
            <button class="success-btn primary" id="success-ok-btn">
                Ok
            </button>
        </div>
    </div>
</div>
